'use strict';

import { BaseGame } from './base__game.js';
/**
 * Class representing the classic game mode.
 */
export class ClassicGame extends BaseGame {
    constructor(snakeSkin, gameSpeed, gameMode) {
        super(snakeSkin, gameSpeed, gameMode);
    }
}